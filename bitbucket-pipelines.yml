image: atlassian/default-image:2

pipelines:
  run-backup:
    - step:
        script:
          - chmod 777 account_repo_backup.sh
          - ./account_repo_backup.sh $userName $appPassword $accountName
          - ls -lah
